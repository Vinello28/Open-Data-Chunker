services:
  etl:
    build: .
    volumes:
      - ./data:/app/data
      - ./public:/app/public
    environment:
      - PYTHONUNBUFFERED=1
    # Mantieni il container attivo per debugging o esecuzione comandi
    command: [ "tail", "-f", "/dev/null" ]
    # Per eseguire comandi specifici: docker compose run --rm etl ...

  web:
    build:
      context: ./web
    ports:
      - "3000:3000"
    volumes:
      - ./public:/app/public
    environment:
      - DATA_DIR=/app/public/parquet
      - EXPORTS_DIR=/app/public/exports
      - CLASSIFIED_DIR=/app/public/classified
      - CACHE_DIR=/app/public/cache
      - PORT=3000
    restart: unless-stopped

  inference-service:
    build:
      context: ./inference-service
      dockerfile: docker/Dockerfile.serve
    ports:
      - "8080:8080"
    volumes:
      - ./inference-service:/app
    environment:
      - INFERENCE_BACKEND=pytorch
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [ gpu ]
